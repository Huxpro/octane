---
'@octanejs/lynx': patch
---

Create fresh deferred native-list cells with their addressed compiled program.

Eligible `list-item` programs now bind once per host container and execute their straight-line
driver when Lynx first requests a cell. Rows with accepted host updates and programs the emitter
cannot prove retain the existing descriptor materializer, including its update and cleanup
semantics.
