---
'octane': patch
---

Extract lazy hook-owner activation and compiler hook-slot composition into the
host-neutral kernel. Renderer cores inject actual child-owner claiming and
active-attempt state, preserving nullable renderer-region probes, keyed owner
identity, and strict hook access diagnostics.
