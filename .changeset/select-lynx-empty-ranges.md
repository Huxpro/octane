---
'@octanejs/rspeedy-plugin': patch
'octane': patch
---

Select the Lynx Block compiled-program product for last-child keyed ranges with an authored `@empty` arm.

Keep nested and non-last ranges on the conservative whole-entry compatibility path.
