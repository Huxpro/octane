---
'octane': patch
'@octanejs/lynx': patch
'@octanejs/rspeedy-plugin': patch
---

Select compiler-proved Lynx `Activity` regions for the Block application product.

Hidden regions now retain component and hook state while disconnecting effects,
refs, and native listeners. Visibility changes publish transactionally after host
acceptance, lower to ordered compatibility commands or a compact `VIS`, and roll
back without exposing rejected work. The compact first screen hides Activity
programs before insertion and reserves—but does not announce—hidden listener
identities so later background adoption keeps the same event cursor.

The paired compiler proof and production fixture now recognize authored
`<Activity>` syntax. Nested and independently overlapping structural ranges,
Suspense, portals, and insertion effects remain conservatively unselected.
