---
"@octanejs/lynx": patch
---

Wrap native Element PAPI handles exposed through `main-thread:ref` with the Lynx `MainThread.Element` API so worklets can read, mutate, query, animate, and invoke their referenced elements on real devices.
