---
'octane': patch
'@octanejs/lynx': patch
'@octanejs/rspeedy-plugin': patch
---

Select Lynx Block applications whose keyed range is followed by retained static content.

Emit the next static sibling in compiler program plans, carry its `(instance, slot)`
address through first-screen paint and compact RUN/MOVE frames, and retain it as the
resident range tail for mount, reorder, clear, and rollback. Promote the capability in
Rspeedy support matrix version 11 while continuing to reject multiple independently
owned ranges under one host.
