---
'octane': patch
'@octanejs/rspack-plugin': patch
'@octanejs/rspeedy-plugin': patch
---

Select immutable same-module component boundaries and inline render props for production Lynx Block applications.

Classify only compiler-proved module-root bindings as supported, carry the fact
through typed Rspack metadata, and keep imported, dynamic, reassigned, or
shadowed components on Universal. Keep root, Provider-child, and sole
component-child descriptor chains free of unaddressable shell programs, and
record template-returning function props as an independently versioned fact.
