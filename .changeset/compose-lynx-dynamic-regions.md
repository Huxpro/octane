---
'@octanejs/lynx': patch
---

Lower compiled `@if` and `@switch` values into keyed dynamic regions.

Preserve same-branch state and clean up replaced branch effects and listeners after host acceptance.
