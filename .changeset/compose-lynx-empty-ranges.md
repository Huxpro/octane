---
'@octanejs/lynx': patch
---

Mount keyed range `@empty` branches as independent compiled templates.

Preserve their local state while empty and clean up effects and listeners when rows replace them.
